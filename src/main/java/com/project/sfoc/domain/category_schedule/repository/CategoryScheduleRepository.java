package com.project.sfoc.domain.category_schedule.repository;

import com.project.sfoc.domain.category_schedule.entity.CategorySchedule;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CategoryScheduleRepository extends JpaRepository<CategorySchedule, Long> {
}
